<!doctype html>
<html lang="ar">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Scoreboard — نسخة للموقع</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --card-bg: rgba(0,0,0,0.55);
      --accent: #ffd400;
      --white: #fff;
      --muted: rgba(255,255,255,0.8);
      font-family: 'Poppins', sans-serif;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0}

    /* background image full-screen */
    .hero{
      position:relative;
      min-height:100vh;
      background-size:cover;
      background-position:center;
      display:flex;
      align-items:center;
      justify-content:center;
    }
    /* dark gradient to make card readable */
    .hero::before{
      content:'';position:absolute;inset:0;
      background:linear-gradient(180deg, rgba(0,0,0,0.35), rgba(0,0,0,0.5));
      z-index:0;
    }

    .card{
      position:relative;z-index:1;
      width:min(920px,94%);
      border-radius:14px;
      padding:28px 26px;
      color:var(--white);
      background:var(--card-bg);
      border:1px solid rgba(255,255,255,0.07);
      box-shadow:0 12px 30px rgba(0,0,0,0.5);
      display:grid;
      grid-template-columns: 1fr auto 1fr;
      gap:18px;
      align-items:center;
    }

    .meta{grid-column:1/-1;text-align:center;font-size:13px;letter-spacing:1px;color:var(--muted);}

    .team{
      display:flex;flex-direction:column;align-items:center;gap:12px;
    }
    .flag{
      width:66px;height:66px;border-radius:50%;overflow:hidden;border:3px solid rgba(255,255,255,0.12);display:flex;align-items:center;justify-content:center;background:#fff;
    }
    .flag img{width:100%;height:100%;object-fit:cover;display:block}
    .team-name{font-weight:600;font-size:15px;text-transform:uppercase;color:var(--white)}

    .scorebox{
      display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;padding:8px 20px;
      min-width:180px;
    }
    .score{font-size:86px;line-height:0.85;font-weight:800;}
    .vs{font-size:14px;color:var(--muted);font-weight:600}
    .status{background:transparent;border-radius:6px;padding:6px 10px;font-weight:700;font-size:12px;color:var(--accent)}

    /* scorers list */
    .scorers{
      position:absolute;left:18px;bottom:14px;color:var(--muted);font-size:14px;display:flex;flex-direction:column;gap:8px;
    }
    .scorer{display:flex;gap:10px;align-items:center}
    .minute{width:36px;height:36;border-radius:8px;background:rgba(255,255,255,0.06);display:flex;align-items:center;justify-content:center;font-weight:700}
    .scorer .name{font-weight:600;color:var(--white)}

    /* small screens */
    @media (max-width:720px){
      .card{grid-template-columns:1fr;gap:12px;padding:18px}
      .score{font-size:56px}
      .scorers{position:static;flex-direction:row;gap:10px;margin-top:6px}
      .scorers .scorer{flex-direction:column;align-items:center}
    }

  </style>
</head>
<body>
  <div class="hero" id="hero" style="background-image:url('background.jpg')">
    <div class="card">
      <div class="meta">WORLD CUP<br>QUALIFIERS</div>

      <div class="team" style="text-align:right">
        <div class="flag"><img src="flag-arg.png" alt="Argentina flag"></div>
        <div class="team-name">Argentina</div>
      </div>

      <div class="scorebox">
        <div class="score" id="score">3 - 0</div>
        <div class="vs">FULL-TIME</div>
      </div>

      <div class="team" style="text-align:left">
        <div class="flag"><img src="flag-ven.png" alt="Venezuela flag"></div>
        <div class="team-name">Venezuela</div>
      </div>

      <!-- scorers list (bottom-left) -->
      <div class="scorers" aria-hidden="false">
        <div class="scorer">
          <div class="minute">39'</div>
          <div class="name">L. Messi</div>
        </div>
        <div class="scorer">
          <div class="minute">76'</div>
          <div class="name">L. Martínez</div>
        </div>
        <div class="scorer">
          <div class="minute">80'</div>
          <div class="name">L. Messi</div>
        </div>
      </div>

    </div>
  </div>

  <!-- <script>
    // small helper: replace data easily from JS if you fetch JSON
    const data = {
      home: {name:'Argentina', flag:'flag-arg.png'},
      away: {name:'Venezuela', flag:'flag-ven.png'},
      score: '3 - 0',
      status: 'FULL-TIME',
      competition: 'WORLD CUP\nQUALIFIERS',
      scorers:[ {min:"39'", name:'L. Messi'},{min:"76'", name:'L. Martínez'},{min:"80'", name:'L. Messi'} ]
    }

    function applyData(d){
      document.querySelector('.meta').textContent = d.competition;
      document.querySelector('.team:nth-of-type(1) .team-name').textContent = d.home.name;
      document.querySelector('.team:nth-of-type(2) .team-name').textContent = d.away.name;
      document.querySelector('.flag img[alt="Argentina flag"]').src = d.home.flag;
      document.querySelector('.flag img[alt="Venezuela flag"]').src = d.away.flag;
      document.getElementById('score').textContent = d.score;
      document.querySelector('.vs').textContent = d.status;

      const scorersContainer = document.querySelector('.scorers');
      scorersContainer.innerHTML = '';
      d.scorers.forEach(s=>{
        const el = document.createElement('div'); el.className='scorer';
        el.innerHTML = `<div class="minute">${s.min}</div><div class="name">${s.name}</div>`;
        scorersContainer.appendChild(el);
      })
    }

    // If you get JSON from your backend replace the hardcoded data.
    // Example: fetch('/api/match/123').then(r=>r.json()).then(applyData)
    applyData(data);
  </script> -->
</body>
</html>
